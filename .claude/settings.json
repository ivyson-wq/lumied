{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || (git commit -m \"chore: auto-save all changes [skip ci]\" && git push origin HEAD) 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Auto-saving all files to GitHub..."
          }
        ]
      }
    ]
  }
}
