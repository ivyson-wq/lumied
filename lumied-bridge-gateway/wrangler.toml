name = "lumied-bridge-gateway"
main = "src/index.ts"
compatibility_date = "2024-09-23"

[vars]
SUPABASE_URL = "https://brgorknbrjlfwvrrlwxj.supabase.co"

# Secrets (wrangler secret put):
# SUPABASE_SERVICE_KEY  — service role para validar bridge_token e atualizar heartbeat
# INTERNAL_SECRET       — bearer p/ /dispatch e /status (edge function passa esse)

[[durable_objects.bindings]]
name = "BRIDGE_DO"
class_name = "BridgeRoom"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["BridgeRoom"]
